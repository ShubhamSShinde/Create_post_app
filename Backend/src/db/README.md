# db Folder

This folder is for database setup.

Simple explanation:

- It connects the app to MongoDB.
- Without this folder, the app cannot save data.

Main file:

- `db.js` connects the project to the database.
