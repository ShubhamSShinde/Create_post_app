# models Folder

This folder defines the data structure.

Simple explanation:

- It tells MongoDB what a post looks like.
- Each post has an image URL and a caption.

Main file:

- `post.model.js` creates the post model.
