# services Folder

This folder contains helper services.

Simple explanation:

- It handles work that is not directly the API route.
- It uploads image files to ImageKit.

Main file:

- `storage.service.js` uploads the image and returns the file result.
