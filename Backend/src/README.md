# src Folder

This folder contains the app logic.

Simple explanation:

- `app.js` is the main Express app.
- `db/` keeps the database connection code.
- `models/` keeps the MongoDB models.
- `services/` keeps helper services like image upload.

This folder is the heart of the backend.
